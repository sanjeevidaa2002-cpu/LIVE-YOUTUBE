# Production build configuration — read automatically by `vite build`.
#
# Both values below are PUBLIC by design and are compiled into the browser
# bundle that every visitor downloads, so committing them exposes nothing
# that visitors cannot already read. Access to your data is enforced by the
# Row Level Security policies in supabase/schema.sql, not by hiding this key.
#
# NEVER add the service_role / secret key to this file.
#
# Environment variables set in the hosting provider (e.g. Vercel Project
# Settings -> Environment Variables) take precedence over these values, so
# you can override them per-environment without editing this file.
VITE_SUPABASE_URL=https://iebxehedsoioapfnpjgq.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_SdzhkUC4VAjNltHylCcevw_wON1FCon
